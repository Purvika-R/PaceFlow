const router=require('express').Router(),c=require('../controllers/taskController'),v=require('../validators/taskValidator'),validate=require('../middleware/validate');router.route('/').post(v.create,validate,c.create).get(c.list);router.patch('/:id/status',v.id(),v.status,validate,c.status);router.route('/:id').get(v.id(),validate,c.get).put(v.id(),v.update,validate,c.update).delete(v.id(),validate,c.remove);module.exports=router;
